---
layout: default
title: "Page Not Found"
permalink: /404.html
sitemap: false
---
{% assign lang = site.active_lang | default: "en" %}
{% if lang == 'ar' %}
  {% assign heading = "404 - الصفحة غير موجودة" %}
  {% assign body = "عذراً، الصفحة التي تبحث عنها غير موجودة أو تم نقلها." %}
  {% assign button_label = "العودة إلى الرئيسية" %}
{% elsif lang == 'es' %}
  {% assign heading = "404 - Pagina no encontrada" %}
  {% assign body = "Lo sentimos, la pagina que buscas no existe o se ha movido." %}
  {% assign button_label = "Volver al inicio" %}
{% elsif lang == 'en-gb' %}
  {% assign heading = "404 - Page Not Found" %}
  {% assign body = "Sorry, the page you are looking for does not exist or has been moved." %}
  {% assign button_label = "Back to Home" %}
{% else %}
  {% assign heading = "404 - Page Not Found" %}
  {% assign body = "Sorry, the page you are looking for does not exist or has been moved." %}
  {% assign button_label = "Back to Home" %}
{% endif %}

{% if lang == 'en' %}
  {% assign home_url = '/' | relative_url %}
{% else %}
  {% assign home_url = '/' | append: lang | append: '/' | relative_url %}
{% endif %}

<section class="max-w-3xl mx-auto px-4 sm:px-6 py-20 sm:py-28 text-center">
  <p class="text-sm font-mono tracking-widest text-violet-600 dark:text-violet-400 mb-3">ERROR 404</p>
  <h1 class="text-3xl sm:text-5xl font-black tracking-tight text-gray-900 dark:text-gray-100 mb-4">{{ heading }}</h1>
  <p class="text-lg text-gray-600 dark:text-gray-400 mb-10">{{ body }}</p>

  <a href="{{ home_url }}"
     class="inline-flex items-center justify-center px-6 py-3 rounded-xl bg-violet-600 hover:bg-violet-700 text-white font-semibold transition-colors">
    {{ button_label }}
  </a>
</section>
